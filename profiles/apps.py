"""
Profiles App - App Configuration

This module contains the configuration for the 'profiles' app.

App Configuration Class:
    ProfilesConfig: Represents the configuration for the 'profiles' app.

Attributes:
    name (str): The name of the app ('profiles').
"""
from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    Represents the configuration for the 'profiles' app.

    Attributes:
        name (str): The name of the app ('profiles').
    """
    name = 'profiles'
